---
- name: install dynatrace agent
  hosts: all
  roles:
    - role: dynatrace-agent
      install_with: chocolatey  # or 'package' for ad-hoc installation
      state: absent # or 'absent' to remove
